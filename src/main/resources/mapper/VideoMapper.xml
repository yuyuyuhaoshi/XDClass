<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="net.xdclass.project.dao.VideoMapper">
    <select id="selectById" parameterType="java.lang.Integer"  resultType="net.xdclass.project.domain.Video">
        select * from video where id = #{id}
    </select>
    
    <select id="selectListByPriceAndName" resultType="net.xdclass.project.domain.Video">
        select * from video where price = #{price,jdbcType=DOUBLE} and name like concat("%", #{name,jdbcType=VARCHAR}, "%")
    </select>

    <insert id="insert" parameterType="net.xdclass.project.domain.Video" useGeneratedKeys="true" keyProperty="id" keyColumn="id">
        INSERT INTO `video` (name, summary, cover_image, price)
        VALUES (#{name,jdbcType=VARCHAR}, #{summary,jdbcType=VARCHAR}, #{coverImage,jdbcType=VARCHAR}, #{price,jdbcType=DOUBLE});
    </insert>
</mapper>